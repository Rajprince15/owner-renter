=================================================================
HOMER - FRONTEND WORKFLOW WITH MOCK DATA
COMPLETE STANDALONE FRONTEND IMPLEMENTATION
=================================================================

╔═══════════════════════════════════════════════════════════════╗
║                    📱 FRONTEND ONLY WORKFLOW 📱               ║
║                                                               ║
║  PURPOSE: Build and test complete frontend independently     ║
║  DATA: All API calls use MOCK data                          ║
║  TESTING: Fully functional UI without backend               ║
║  INTEGRATION: Designed to seamlessly connect to backend     ║
║           when ready (just swap mock services)               ║
╚═══════════════════════════════════════════════════════════════╝

=================================================================
TABLE OF CONTENTS
=================================================================
1. Tech Stack & Setup
2. Project Structure
3. Mock API Service Layer (Complete API Contracts)
4. Mock Data Structures (Based on Database Schema)
5. Page Components & Routing
6. Reusable UI Components
7. State Management
8. Complete Implementation Guide
9. Testing Without Backend

=================================================================
1. TECH STACK & SETUP
=================================================================

FRONTEND STACK:
- React.js 18+ (with Hooks)
- React Router v6 (Navigation)
- Tailwind CSS (Styling)
- Axios (API calls - will use mock service)
- React Context API (State management)
- React Hook Form (Form handling)
- Chart.js / Recharts (Analytics charts)
- Lucide React / Heroicons (Icons)

SETUP COMMANDS:
```bash
cd /app/frontend
yarn add react-router-dom axios react-hook-form chart.js react-chartjs-2 lucide-react
```

ENVIRONMENT VARIABLES (.env):
```
REACT_APP_BACKEND_URL=http://localhost:8001/api
REACT_APP_USE_MOCK_DATA=true  # Set to false when backend is ready
```

=================================================================
2. PROJECT STRUCTURE
=================================================================

/app/frontend/
├── public/
│   ├── index.html
│   └── assets/
│       └── images/
├── src/
│   ├── index.js                    # Entry point
│   ├── App.js                      # Main app component
│   ├── App.css                     # Global styles
│   │
│   ├── pages/                      # All page components
│   │   ├── LandingPage.js
│   │   ├── Login.js
│   │   ├── Signup.js
│   │   ├── Search.js
│   │   ├── PropertyDetail.js
│   │   ├── About.js
│   │   ├── Pricing.js
│   │   │
│   │   ├── renter/
│   │   │   ├── Dashboard.js
│   │   │   ├── Profile.js
│   │   │   ├── Subscription.js
│   │   │   ├── Shortlists.js
│   │   │   ├── Chats.js
│   │   │   ├── VerificationUpload.js
│   │   │   └── PrivacySettings.js
│   │   │
│   │   └── owner/
│   │       ├── Dashboard.js
│   │       ├── MyProperties.js
│   │       ├── AddProperty.js
│   │       ├── EditProperty.js
│   │       ├── PropertyAnalytics.js
│   │       ├── Verification.js
│   │       ├── ReverseMarketplace.js
│   │       └── Chats.js
│   │
│   ├── components/                 # Reusable components
│   │   ├── layout/
│   │   │   ├── Navbar.js
│   │   │   ├── Footer.js
│   │   │   └── Sidebar.js
│   │   │
│   │   ├── common/
│   │   │   ├── Button.js
│   │   │   ├── Input.js
│   │   │   ├── Modal.js
│   │   │   ├── Card.js
│   │   │   ├── Badge.js
│   │   │   ├── LoadingSpinner.js
│   │   │   ├── Skeleton.js
│   │   │   └── Toast.js
│   │   │
│   │   ├── property/
│   │   │   ├── PropertyCard.js
│   │   │   ├── PropertyForm.js
│   │   │   ├── PropertyGallery.js
│   │   │   ├── SearchFilters.js
│   │   │   └── LifestyleScoreBadge.js
│   │   │
│   │   ├── chat/
│   │   │   ├── ChatList.js
│   │   │   ├── ChatWindow.js
│   │   │   └── MessageBubble.js
│   │   │
│   │   ├── verification/
│   │   │   ├── DocumentUpload.js
│   │   │   └── VerificationStatusTracker.js
│   │   │
│   │   ├── payment/
│   │   │   ├── MockPaymentModal.js
│   │   │   ├── PricingCard.js
│   │   │   └── SubscriptionBanner.js
│   │   │
│   │   └── upsell/
│   │       ├── UpgradeModal.js
│   │       ├── ContactLimitIndicator.js
│   │       └── UpgradePrompt.js
│   │
│   ├── services/                   # Mock API services
│   │   ├── mockApi.js             # Mock data and API simulator
│   │   ├── authService.js         # Authentication service
│   │   ├── propertyService.js     # Property CRUD service
│   │   ├── searchService.js       # Search service
│   │   ├── chatService.js         # Chat service
│   │   ├── paymentService.js      # Payment service
│   │   ├── verificationService.js # Verification service
│   │   └── analyticsService.js    # Analytics service
│   │
│   ├── context/                    # React Context
│   │   ├── AuthContext.js
│   │   ├── ThemeContext.js
│   │   └── NotificationContext.js
│   │
│   ├── hooks/                      # Custom hooks
│   │   ├── useAuth.js
│   │   ├── useDebounce.js
│   │   └── useLocalStorage.js
│   │
│   ├── utils/                      # Utility functions
│   │   ├── dateFormatter.js
│   │   ├── priceFormatter.js
│   │   └── validation.js
│   │
│   └── constants/                  # Constants
│       ├── apiEndpoints.js
│       └── appConstants.js

=================================================================
3. MOCK API SERVICE LAYER (COMPLETE API CONTRACTS)
=================================================================

This section defines ALL API endpoints that the backend will implement.
For frontend development, these return mock data.

**IMPORTANT: CENTRALIZED MOCK DATA**
All mock data is now centralized in a single file for easier management:
- FILE: /app/frontend/src/services/mockData.js
- Contains: mockUsers, mockProperties, mockChats, mockTransactions, mockShortlists, mockNotifications
- Includes helper functions for data access
- All service files import mock data from this centralized location
- When backend is ready, only mockApi.js needs to change USE_MOCK flag

FILE: /app/frontend/src/services/mockApi.js
-------------------------------------------

```javascript
// Mock API Configuration
const USE_MOCK = process.env.REACT_APP_USE_MOCK_DATA === 'true';
const API_BASE_URL = process.env.REACT_APP_BACKEND_URL;

// Simulated network delay for realistic testing
const MOCK_DELAY = 800; // milliseconds

// Helper to simulate API call
const mockApiCall = (data, delay = MOCK_DELAY) => {
  return new Promise((resolve) => {
    setTimeout(() => resolve(data), delay);
  });
};

// Helper to simulate API error
const mockApiError = (message, status = 400, delay = MOCK_DELAY) => {
  return new Promise((_, reject) => {
    setTimeout(() => reject({ 
      response: { 
        data: { message }, 
        status 
      } 
    }), delay);
  });
};

export { USE_MOCK, API_BASE_URL, mockApiCall, mockApiError };
```

=================================================================
3.1 AUTHENTICATION API (Mock)
=================================================================

FILE: /app/frontend/src/services/authService.js
------------------------------------------------

**NOTE: This service now imports mock data from /app/frontend/src/services/mockData.js**
**See mockData.js for complete data structures**

```javascript
import { USE_MOCK, mockApiCall, mockApiError } from './mockApi';
import axios from 'axios';
import { mockUsers, getUserByCredentials, getUserById } from './mockData';

// mockUsers is now imported from mockData.js
// Example mockUsers structure (see mockData.js for complete data):
/*
let mockUsers = [
  {
    user_id: 'user_001_renter_free',
    email: 'renter@test.com',
    phone: '+919876543210',
    password_hash: 'password123', // In real backend, this is hashed
    user_type: 'renter',
    full_name: 'Raj Kumar',
    subscription_tier: 'free',
    contacts_used: 2,
    is_verified_renter: false,
    renter_verification_status: 'none',
    created_at: new Date().toISOString()
  },
  {
    user_id: 'user_002_renter_premium',
    email: 'premium@test.com',
    phone: '+919876543211',
    password_hash: 'password123',
    user_type: 'renter',
    full_name: 'Priya Sharma',
    subscription_tier: 'premium',
    subscription_start: new Date().toISOString(),
    subscription_end: new Date(Date.now() + 90 * 24 * 60 * 60 * 1000).toISOString(),
    contacts_used: 15,
    is_verified_renter: true,
    renter_verification_status: 'verified',
    renter_preferences: {
      budget_min: 20000,
      budget_max: 35000,
      bhk_type: ['2BHK', '3BHK'],
      preferred_locations: ['Indiranagar', 'Whitefield']
    },
    employment_info: {
      company_name: 'Tech Corp',
      designation: 'Senior Engineer',
      employment_type: 'salaried',
      annual_income: 1500000
    },
    created_at: new Date().toISOString()
  },
  {
    user_id: 'user_003_owner',
    email: 'owner@test.com',
    phone: '+919876543212',
    password_hash: 'password123',
    user_type: 'owner',
    full_name: 'Amit Patel',
    is_verified_owner: true,
    owner_verification_status: 'verified',
    created_at: new Date().toISOString()
  }
];

// API ENDPOINT: POST /api/auth/register
export const register = async (userData) => {
  if (USE_MOCK) {
    // Check if email/phone already exists
    const existingUser = mockUsers.find(
      u => u.email === userData.email || u.phone === userData.phone
    );
    
    if (existingUser) {
      return mockApiError('Email or phone already registered', 400);
    }
    
    // Create new user
    const newUser = {
      user_id: `user_${Date.now()}`,
      ...userData,
      password_hash: userData.password, // Mock - real backend hashes this
      subscription_tier: userData.user_type === 'renter' ? 'free' : null,
      contacts_used: 0,
      is_verified_renter: false,
      is_verified_owner: false,
      renter_verification_status: 'none',
      owner_verification_status: 'none',
      is_active: true,
      created_at: new Date().toISOString()
    };
    
    mockUsers.push(newUser);
    
    // Generate mock JWT token
    const token = `mock_jwt_${newUser.user_id}_${Date.now()}`;
    
    return mockApiCall({
      user_id: newUser.user_id,
      token: token,
      user_type: newUser.user_type,
      user: { ...newUser, password_hash: undefined }
    });
  }
  
  // Real API call
  return axios.post('/api/auth/register', userData);
};

// API ENDPOINT: POST /api/auth/login
export const login = async (credentials) => {
  if (USE_MOCK) {
    // Find user by email or phone
    const user = mockUsers.find(
      u => (u.email === credentials.email || u.phone === credentials.email) &&
           u.password_hash === credentials.password
    );
    
    if (!user) {
      return mockApiError('Invalid credentials', 401);
    }
    
    // Generate mock JWT token
    const token = `mock_jwt_${user.user_id}_${Date.now()}`;
    
    return mockApiCall({
      user_id: user.user_id,
      token: token,
      user_type: user.user_type,
      user: { ...user, password_hash: undefined }
    });
  }
  
  // Real API call
  return axios.post('/api/auth/login', credentials);
};

// API ENDPOINT: POST /api/auth/logout
export const logout = async () => {
  if (USE_MOCK) {
    return mockApiCall({ message: 'Logged out successfully' });
  }
  
  return axios.post('/api/auth/logout');
};

// API ENDPOINT: GET /api/auth/me
export const getCurrentUser = async () => {
  if (USE_MOCK) {
    // Get user from token stored in localStorage
    const token = localStorage.getItem('token');
    if (!token) {
      return mockApiError('Not authenticated', 401);
    }
    
    // Extract user_id from mock token
    const userId = token.split('_')[2];
    const user = mockUsers.find(u => u.user_id.includes(userId));
    
    if (!user) {
      return mockApiError('User not found', 404);
    }
    
    return mockApiCall({ ...user, password_hash: undefined });
  }
  
  return axios.get('/api/auth/me');
};

// API ENDPOINT: GET /api/users/profile
export const getProfile = async () => {
  if (USE_MOCK) {
    return getCurrentUser();
  }
  
  return axios.get('/api/users/profile');
};

// API ENDPOINT: PUT /api/users/profile
export const updateProfile = async (profileData) => {
  if (USE_MOCK) {
    const token = localStorage.getItem('token');
    const userId = token.split('_')[2];
    const userIndex = mockUsers.findIndex(u => u.user_id.includes(userId));
    
    if (userIndex === -1) {
      return mockApiError('User not found', 404);
    }
    
    mockUsers[userIndex] = { ...mockUsers[userIndex], ...profileData };
    
    return mockApiCall({ ...mockUsers[userIndex], password_hash: undefined });
  }
  
  return axios.put('/api/users/profile', profileData);
};

// API ENDPOINT: GET /api/users/renter/contacts-remaining
export const getContactsRemaining = async () => {
  if (USE_MOCK) {
    const token = localStorage.getItem('token');
    const userId = token.split('_')[2];
    const user = mockUsers.find(u => u.user_id.includes(userId));
    
    if (!user) {
      return mockApiError('User not found', 404);
    }
    
    const isPremium = user.subscription_tier === 'premium';
    const contactsLimit = isPremium ? 'unlimited' : 5;
    
    return mockApiCall({
      contacts_used: user.contacts_used,
      contacts_limit: contactsLimit,
      is_premium: isPremium
    });
  }
  
  return axios.get('/api/users/renter/contacts-remaining');
};
```

=================================================================
3.2 PROPERTY API (Mock)
=================================================================

FILE: /app/frontend/src/services/propertyService.js
----------------------------------------------------

**NOTE: This service now imports mock data from /app/frontend/src/services/mockData.js**
**See mockData.js for complete data structures and propertyService.js for full implementation**

```javascript
import { USE_MOCK, mockApiCall, mockApiError } from './mockApi';
import axios from 'axios';
import { 
  mockProperties, 
  getPropertyById, 
  getPropertiesByOwner, 
  generateMockProperty 
} from './mockData';

// mockProperties is now imported from mockData.js
// Example mockProperties structure (see mockData.js for complete data):
/*
let mockProperties = [
  {
    property_id: 'prop_001',
    owner_id: 'user_003_owner',
    title: 'Spacious 2BHK Apartment in Koramangala',
    description: 'Well-maintained 2BHK apartment with all modern amenities.',
    property_type: 'apartment',
    bhk_type: '2BHK',
    rent: 22000,
    security_deposit: 44000,
    maintenance_charges: 2000,
    location: {
      address: '123, 4th Block, Koramangala',
      city: 'Bangalore',
      state: 'Karnataka',
      pincode: '560034',
      latitude: 12.9352,
      longitude: 77.6245,
      locality: 'Koramangala',
      landmarks: ['Sony Signal', 'Forum Mall']
    },
    details: {
      carpet_area: 1100,
      total_floors: 5,
      floor_number: 3,
      furnishing: 'semi-furnished',
      parking: { car: 1, bike: 1 },
      amenities: ['lift', 'power_backup', 'security'],
      bathrooms: 2,
      balconies: 1,
      facing: 'east',
      age_of_property: 5,
      available_from: '2025-02-15',
      preferred_tenants: ['family', 'bachelor'],
      pets_allowed: false,
      vegetarian_only: false
    },
    images: [
      'https://images.unsplash.com/photo-1502672260266-1c1ef2d93688?w=800',
      'https://images.unsplash.com/photo-1560448204-e02f11c3d0e2?w=800'
    ],
    is_verified: false,
    verification_tier: 'free',
    status: 'active',
    analytics: {
      total_views: 45,
      premium_views: 8,
      total_contacts: 3,
      shortlisted_count: 12,
      last_viewed: new Date().toISOString()
    },
    created_at: new Date(Date.now() - 10 * 24 * 60 * 60 * 1000).toISOString()
  },
  {
    property_id: 'prop_002',
    owner_id: 'user_003_owner',
    title: 'Luxury 3BHK with Park View in Indiranagar',
    description: 'Premium 3BHK apartment with excellent ventilation and park view.',
    property_type: 'apartment',
    bhk_type: '3BHK',
    rent: 35000,
    security_deposit: 70000,
    maintenance_charges: 3500,
    location: {
      address: '456, 100 Feet Road, Indiranagar',
      city: 'Bangalore',
      state: 'Karnataka',
      pincode: '560038',
      latitude: 12.9716,
      longitude: 77.6412,
      locality: 'Indiranagar',
      landmarks: ['Metro Station', 'CMH Road', 'Central Park']
    },
    details: {
      carpet_area: 1500,
      total_floors: 8,
      floor_number: 6,
      furnishing: 'furnished',
      parking: { car: 2, bike: 2 },
      amenities: ['gym', 'swimming_pool', 'lift', 'power_backup', 'security', 'park', 'club_house'],
      bathrooms: 3,
      balconies: 2,
      facing: 'north',
      age_of_property: 2,
      available_from: '2025-02-01',
      preferred_tenants: ['family'],
      pets_allowed: true,
      vegetarian_only: false
    },
    images: [
      'https://images.unsplash.com/photo-1545324418-cc1a3fa10c00?w=800',
      'https://images.unsplash.com/photo-1512917774080-9991f1c4c750?w=800',
      'https://images.unsplash.com/photo-1502672260266-1c1ef2d93688?w=800'
    ],
    is_verified: true,
    verification_tier: 'verified',
    verification_date: new Date(Date.now() - 5 * 24 * 60 * 60 * 1000).toISOString(),
    lifestyle_data: {
      aqi_score: 55,
      noise_level: 52,
      walkability_score: 85,
      nearby_parks: [
        { name: 'Indiranagar Park', distance: 0.3 },
        { name: 'Central Park', distance: 0.8 }
      ],
      nearby_hospitals: [{ name: 'Apollo Hospital', distance: 2.5 }],
      nearby_schools: [{ name: 'National Public School', distance: 1.2 }],
      nearby_malls: [{ name: 'Garuda Mall', distance: 1.5 }],
      public_transport_score: 90,
      safety_score: 82,
      calculated_at: new Date().toISOString(),
      is_mock: true
    },
    analytics: {
      total_views: 245,
      premium_views: 78,
      total_contacts: 15,
      shortlisted_count: 42,
      last_viewed: new Date().toISOString()
    },
    status: 'active',
    created_at: new Date(Date.now() - 20 * 24 * 60 * 60 * 1000).toISOString()
  },
  {
    property_id: 'prop_003',
    owner_id: 'user_003_owner',
    title: 'Cozy 1BHK Near HSR Layout',
    description: 'Affordable 1BHK apartment perfect for bachelors.',
    property_type: 'apartment',
    bhk_type: '1BHK',
    rent: 12000,
    security_deposit: 24000,
    maintenance_charges: 1000,
    location: {
      address: '789, Sector 1, HSR Layout',
      city: 'Bangalore',
      state: 'Karnataka',
      pincode: '560102',
      latitude: 12.9121,
      longitude: 77.6446,
      locality: 'HSR Layout',
      landmarks: ['27th Main Road', 'BDA Complex']
    },
    details: {
      carpet_area: 600,
      total_floors: 4,
      floor_number: 2,
      furnishing: 'semi-furnished',
      parking: { car: 0, bike: 1 },
      amenities: ['lift', 'security'],
      bathrooms: 1,
      balconies: 1,
      facing: 'west',
      age_of_property: 7,
      available_from: '2025-02-20',
      preferred_tenants: ['bachelor'],
      pets_allowed: false,
      vegetarian_only: true
    },
    images: [
      'https://images.unsplash.com/photo-1522708323590-d24dbb6b0267?w=800'
    ],
    is_verified: true,
    verification_tier: 'verified',
    lifestyle_data: {
      aqi_score: 68,
      noise_level: 62,
      walkability_score: 70,
      nearby_parks: [{ name: 'HSR Lake', distance: 1.2 }],
      nearby_hospitals: [{ name: 'Fortis Hospital', distance: 2.0 }],
      nearby_schools: [],
      nearby_malls: [],
      public_transport_score: 65,
      safety_score: 75,
      calculated_at: new Date().toISOString(),
      is_mock: true
    },
    analytics: {
      total_views: 89,
      premium_views: 25,
      total_contacts: 6,
      shortlisted_count: 18,
      last_viewed: new Date().toISOString()
    },
    status: 'active',
    created_at: new Date(Date.now() - 15 * 24 * 60 * 60 * 1000).toISOString()
  }
];

// API ENDPOINT: POST /api/properties/create
export const createProperty = async (propertyData) => {
  if (USE_MOCK) {
    const token = localStorage.getItem('token');
    const userId = token.split('_')[2];
    
    const newProperty = {
      property_id: `prop_${Date.now()}`,
      owner_id: userId,
      ...propertyData,
      is_verified: false,
      verification_tier: 'free',
      status: 'active',
      analytics: {
        total_views: 0,
        premium_views: 0,
        total_contacts: 0,
        shortlisted_count: 0,
        last_viewed: null
      },
      created_at: new Date().toISOString()
    };
    
    mockProperties.push(newProperty);
    
    return mockApiCall({
      property_id: newProperty.property_id,
      property: newProperty
    });
  }
  
  return axios.post('/api/properties/create', propertyData);
};

// API ENDPOINT: GET /api/properties/search
export const searchProperties = async (filters = {}) => {
  if (USE_MOCK) {
    let results = [...mockProperties];
    
    // Apply filters
    if (filters.city) {
      results = results.filter(p => 
        p.location.city.toLowerCase().includes(filters.city.toLowerCase())
      );
    }
    
    if (filters.min_price) {
      results = results.filter(p => p.rent >= filters.min_price);
    }
    
    if (filters.max_price) {
      results = results.filter(p => p.rent <= filters.max_price);
    }
    
    if (filters.bhk_type) {
      results = results.filter(p => p.bhk_type === filters.bhk_type);
    }
    
    if (filters.location) {
      results = results.filter(p => 
        p.location.locality.toLowerCase().includes(filters.location.toLowerCase())
      );
    }
    
    // Sort
    if (filters.sort_by === 'price_low') {
      results.sort((a, b) => a.rent - b.rent);
    } else if (filters.sort_by === 'price_high') {
      results.sort((a, b) => b.rent - a.rent);
    } else if (filters.sort_by === 'recent') {
      results.sort((a, b) => new Date(b.created_at) - new Date(a.created_at));
    } else {
      // Default: verified first, then by date
      results.sort((a, b) => {
        if (a.is_verified && !b.is_verified) return -1;
        if (!a.is_verified && b.is_verified) return 1;
        return new Date(b.created_at) - new Date(a.created_at);
      });
    }
    
    // Pagination
    const page = filters.page || 1;
    const limit = filters.limit || 20;
    const startIndex = (page - 1) * limit;
    const endIndex = startIndex + limit;
    const paginatedResults = results.slice(startIndex, endIndex);
    
    return mockApiCall({
      properties: paginatedResults,
      total_count: results.length,
      page: page,
      has_more: endIndex < results.length
    });
  }
  
  return axios.get('/api/properties/search', { params: filters });
};

// API ENDPOINT: GET /api/properties/lifestyle-search
export const lifestyleSearch = async (filters) => {
  if (USE_MOCK) {
    // Only verified properties with lifestyle data
    let results = mockProperties.filter(p => 
      p.is_verified && p.lifestyle_data
    );
    
    // Apply lifestyle filters
    if (filters.max_aqi) {
      results = results.filter(p => p.lifestyle_data.aqi_score <= filters.max_aqi);
    }
    
    if (filters.max_noise) {
      results = results.filter(p => p.lifestyle_data.noise_level <= filters.max_noise);
    }
    
    if (filters.min_walkability) {
      results = results.filter(p => p.lifestyle_data.walkability_score >= filters.min_walkability);
    }
    
    if (filters.near_parks) {
      results = results.filter(p => p.lifestyle_data.nearby_parks.length > 0);
    }
    
    // Apply standard filters
    if (filters.budget) {
      results = results.filter(p => p.rent <= filters.budget);
    }
    
    if (filters.bhk) {
      results = results.filter(p => p.bhk_type === filters.bhk);
    }
    
    return mockApiCall({
      properties: results,
      total_count: results.length
    });
  }
  
  return axios.get('/api/properties/lifestyle-search', { params: filters });
};

// API ENDPOINT: GET /api/properties/:property_id
export const getPropertyDetail = async (propertyId) => {
  if (USE_MOCK) {
    const property = mockProperties.find(p => p.property_id === propertyId);
    
    if (!property) {
      return mockApiError('Property not found', 404);
    }
    
    // Increment view count
    property.analytics.total_views++;
    property.analytics.last_viewed = new Date().toISOString();
    
    return mockApiCall(property);
  }
  
  return axios.get(`/api/properties/${propertyId}`);
};

// API ENDPOINT: PUT /api/properties/:property_id
export const updateProperty = async (propertyId, propertyData) => {
  if (USE_MOCK) {
    const index = mockProperties.findIndex(p => p.property_id === propertyId);
    
    if (index === -1) {
      return mockApiError('Property not found', 404);
    }
    
    mockProperties[index] = {
      ...mockProperties[index],
      ...propertyData,
      updated_at: new Date().toISOString()
    };
    
    return mockApiCall(mockProperties[index]);
  }
  
  return axios.put(`/api/properties/${propertyId}`, propertyData);
};

// API ENDPOINT: DELETE /api/properties/:property_id
export const deleteProperty = async (propertyId) => {
  if (USE_MOCK) {
    const index = mockProperties.findIndex(p => p.property_id === propertyId);
    
    if (index === -1) {
      return mockApiError('Property not found', 404);
    }
    
    mockProperties.splice(index, 1);
    
    return mockApiCall({ message: 'Property deleted successfully' });
  }
  
  return axios.delete(`/api/properties/${propertyId}`);
};

// API ENDPOINT: GET /api/properties/my-properties
export const getMyProperties = async () => {
  if (USE_MOCK) {
    const token = localStorage.getItem('token');
    const userId = token.split('_')[2];
    
    const myProperties = mockProperties.filter(p => p.owner_id.includes(userId));
    
    return mockApiCall(myProperties);
  }
  
  return axios.get('/api/properties/my-properties');
};

// API ENDPOINT: POST /api/properties/:property_id/upload-images
export const uploadPropertyImages = async (propertyId, files) => {
  if (USE_MOCK) {
    // Simulate image upload
    const imageUrls = files.map((file, index) => 
      `https://images.unsplash.com/photo-${Date.now()}-${index}?w=800`
    );
    
    const property = mockProperties.find(p => p.property_id === propertyId);
    if (property) {
      property.images = [...(property.images || []), ...imageUrls];
    }
    
    return mockApiCall({ image_urls: imageUrls });
  }
  
  const formData = new FormData();
  files.forEach(file => formData.append('images', file));
  
  return axios.post(`/api/properties/${propertyId}/upload-images`, formData);
};

// API ENDPOINT: GET /api/properties/:property_id/analytics
export const getPropertyAnalytics = async (propertyId) => {
  if (USE_MOCK) {
    const property = mockProperties.find(p => p.property_id === propertyId);
    
    if (!property) {
      return mockApiError('Property not found', 404);
    }
    
    return mockApiCall(property.analytics);
  }
  
  return axios.get(`/api/properties/${propertyId}/analytics`);
};
```

=================================================================
3.3 CHAT API (Mock)
=================================================================

FILE: /app/frontend/src/services/chatService.js
------------------------------------------------

```javascript
import { USE_MOCK, mockApiCall, mockApiError } from './mockApi';
import axios from 'axios';

// MOCK CHATS DATA
let mockChats = [
  {
    chat_id: 'chat_001',
    property_id: 'prop_002',
    renter_id: 'user_002_renter_premium',
    owner_id: 'user_003_owner',
    initiated_by: 'renter',
    created_at: new Date(Date.now() - 2 * 24 * 60 * 60 * 1000).toISOString(),
    last_message_at: new Date(Date.now() - 1 * 60 * 60 * 1000).toISOString(),
    status: 'active',
    messages: [
      {
        message_id: 'msg_001',
        sender_id: 'user_002_renter_premium',
        sender_type: 'renter',
        message: 'Hi, I am interested in this property. Is it still available?',
        timestamp: new Date(Date.now() - 2 * 24 * 60 * 60 * 1000).toISOString(),
        is_read: true,
        attachments: []
      },
      {
        message_id: 'msg_002',
        sender_id: 'user_003_owner',
        sender_type: 'owner',
        message: 'Yes, it is available. Would you like to schedule a visit?',
        timestamp: new Date(Date.now() - 2 * 24 * 60 * 60 * 1000 + 60 * 60 * 1000).toISOString(),
        is_read: true,
        attachments: []
      },
      {
        message_id: 'msg_003',
        sender_id: 'user_002_renter_premium',
        sender_type: 'renter',
        message: 'Yes, I would like to visit this weekend. Is Saturday 2 PM good?',
        timestamp: new Date(Date.now() - 1 * 60 * 60 * 1000).toISOString(),
        is_read: false,
        attachments: []
      }
    ]
  }
];

// API ENDPOINT: POST /api/chats/initiate
export const initiateChat = async (propertyId, initialMessage) => {
  if (USE_MOCK) {
    const token = localStorage.getItem('token');
    const userId = token.split('_')[2];
    
    // Get user to check contacts
    const user = JSON.parse(localStorage.getItem('user'));
    
    // Check if free tier and at limit
    if (user.subscription_tier === 'free' && user.contacts_used >= 5) {
      return mockApiError('Contact limit reached. Upgrade to premium.', 403);
    }
    
    // Check if chat already exists
    const existingChat = mockChats.find(
      c => c.property_id === propertyId && c.renter_id.includes(userId)
    );
    
    if (existingChat) {
      return mockApiCall({ chat_id: existingChat.chat_id });
    }
    
    // Create new chat
    const newChat = {
      chat_id: `chat_${Date.now()}`,
      property_id: propertyId,
      renter_id: userId,
      owner_id: 'user_003_owner', // In real app, get from property
      initiated_by: 'renter',
      created_at: new Date().toISOString(),
      last_message_at: new Date().toISOString(),
      status: 'active',
      messages: [
        {
          message_id: `msg_${Date.now()}`,
          sender_id: userId,
          sender_type: 'renter',
          message: initialMessage,
          timestamp: new Date().toISOString(),
          is_read: false,
          attachments: []
        }
      ]
    };
    
    mockChats.push(newChat);
    
    // Increment contacts_used
    user.contacts_used++;
    localStorage.setItem('user', JSON.stringify(user));
    
    return mockApiCall({ chat_id: newChat.chat_id });
  }
  
  return axios.post('/api/chats/initiate', { propertyId, initialMessage });
};

// API ENDPOINT: GET /api/chats
export const getChats = async () => {
  if (USE_MOCK) {
    const token = localStorage.getItem('token');
    const userId = token.split('_')[2];
    
    const userChats = mockChats.filter(
      c => c.renter_id.includes(userId) || c.owner_id.includes(userId)
    );
    
    return mockApiCall(userChats);
  }
  
  return axios.get('/api/chats');
};

// API ENDPOINT: GET /api/chats/:chat_id
export const getChatDetail = async (chatId) => {
  if (USE_MOCK) {
    const chat = mockChats.find(c => c.chat_id === chatId);
    
    if (!chat) {
      return mockApiError('Chat not found', 404);
    }
    
    // Mark messages as read
    chat.messages.forEach(msg => {
      msg.is_read = true;
    });
    
    return mockApiCall(chat);
  }
  
  return axios.get(`/api/chats/${chatId}`);
};

// API ENDPOINT: POST /api/chats/:chat_id/messages
export const sendMessage = async (chatId, messageText, attachments = []) => {
  if (USE_MOCK) {
    const token = localStorage.getItem('token');
    const userId = token.split('_')[2];
    
    const chat = mockChats.find(c => c.chat_id === chatId);
    
    if (!chat) {
      return mockApiError('Chat not found', 404);
    }
    
    const senderType = chat.renter_id.includes(userId) ? 'renter' : 'owner';
    
    const newMessage = {
      message_id: `msg_${Date.now()}`,
      sender_id: userId,
      sender_type: senderType,
      message: messageText,
      timestamp: new Date().toISOString(),
      is_read: false,
      attachments: attachments
    };
    
    chat.messages.push(newMessage);
    chat.last_message_at = newMessage.timestamp;
    
    return mockApiCall({ message_id: newMessage.message_id });
  }
  
  return axios.post(`/api/chats/${chatId}/messages`, {
    message: messageText,
    attachments
  });
};

// API ENDPOINT: GET /api/chats/unread-count
export const getUnreadCount = async () => {
  if (USE_MOCK) {
    const token = localStorage.getItem('token');
    const userId = token.split('_')[2];
    
    const userChats = mockChats.filter(
      c => c.renter_id.includes(userId) || c.owner_id.includes(userId)
    );
    
    let unreadCount = 0;
    userChats.forEach(chat => {
      const unreadMessages = chat.messages.filter(
        msg => !msg.is_read && !msg.sender_id.includes(userId)
      );
      unreadCount += unreadMessages.length;
    });
    
    return mockApiCall({ unread_count: unreadCount });
  }
  
  return axios.get('/api/chats/unread-count');
};
```

=================================================================
3.4 PAYMENT API (Mock)
=================================================================

FILE: /app/frontend/src/services/paymentService.js
---------------------------------------------------

```javascript
import { USE_MOCK, mockApiCall, mockApiError } from './mockApi';
import axios from 'axios';

// MOCK TRANSACTIONS DATA
let mockTransactions = [];

// API ENDPOINT: POST /api/payments/create-order
export const createPaymentOrder = async (type, metadata) => {
  if (USE_MOCK) {
    const orderId = `mock_order_${Date.now()}`;
    const amount = type === 'renter_subscription' ? 750 : 2000;
    
    return mockApiCall({
      order_id: orderId,
      amount: amount,
      currency: 'INR',
      is_mock: true,
      payment_gateway_data: {
        key_id: 'mock_razorpay_key',
        name: 'Homer',
        description: type === 'renter_subscription' ? 'Premium Subscription' : 'Property Verification'
      }
    });
  }
  
  return axios.post('/api/payments/create-order', { type, metadata });
};

// API ENDPOINT: POST /api/payments/verify
export const verifyPayment = async (paymentId, orderId, signature) => {
  if (USE_MOCK) {
    const token = localStorage.getItem('token');
    const userId = token.split('_')[2];
    
    // Simulate successful payment verification
    const transactionId = `txn_${Date.now()}`;
    
    const transaction = {
      transaction_id: transactionId,
      user_id: userId,
      transaction_type: orderId.includes('subscription') ? 'renter_subscription' : 'property_verification',
      amount: orderId.includes('subscription') ? 750 : 2000,
      currency: 'INR',
      payment_gateway: 'razorpay',
      payment_id: paymentId,
      order_id: orderId,
      payment_status: 'success',
      is_mock: true,
      created_at: new Date().toISOString(),
      metadata: {}
    };
    
    mockTransactions.push(transaction);
    
    // Update user if subscription
    if (transaction.transaction_type === 'renter_subscription') {
      const user = JSON.parse(localStorage.getItem('user'));
      user.subscription_tier = 'premium';
      user.subscription_start = new Date().toISOString();
      user.subscription_end = new Date(Date.now() + 90 * 24 * 60 * 60 * 1000).toISOString();
      user.contacts_used = 0;
      localStorage.setItem('user', JSON.stringify(user));
    }
    
    return mockApiCall({
      success: true,
      transaction_id: transactionId
    });
  }
  
  return axios.post('/api/payments/verify', {
    payment_id: paymentId,
    order_id: orderId,
    signature: signature
  });
};

// API ENDPOINT: GET /api/payments/history
export const getPaymentHistory = async () => {
  if (USE_MOCK) {
    const token = localStorage.getItem('token');
    const userId = token.split('_')[2];
    
    const userTransactions = mockTransactions.filter(t => t.user_id.includes(userId));
    
    return mockApiCall(userTransactions);
  }
  
  return axios.get('/api/payments/history');
};
```

=================================================================
3.5 SHORTLIST & OTHER APIs (Mock)
=================================================================

FILE: /app/frontend/src/services/shortlistService.js
-----------------------------------------------------

```javascript
import { USE_MOCK, mockApiCall, mockApiError } from './mockApi';
import axios from 'axios';

let mockShortlists = [];

// API ENDPOINT: POST /api/shortlists/add
export const addToShortlist = async (propertyId, notes = '') => {
  if (USE_MOCK) {
    const token = localStorage.getItem('token');
    const userId = token.split('_')[2];
    
    // Check if already shortlisted
    const existing = mockShortlists.find(
      s => s.property_id === propertyId && s.user_id.includes(userId)
    );
    
    if (existing) {
      return mockApiError('Property already in shortlist', 400);
    }
    
    const shortlist = {
      shortlist_id: `shortlist_${Date.now()}`,
      user_id: userId,
      property_id: propertyId,
      notes: notes,
      created_at: new Date().toISOString()
    };
    
    mockShortlists.push(shortlist);
    
    return mockApiCall({ shortlist_id: shortlist.shortlist_id });
  }
  
  return axios.post('/api/shortlists/add', { property_id: propertyId, notes });
};

// API ENDPOINT: GET /api/shortlists
export const getShortlists = async () => {
  if (USE_MOCK) {
    const token = localStorage.getItem('token');
    const userId = token.split('_')[2];
    
    const userShortlists = mockShortlists.filter(s => s.user_id.includes(userId));
    
    return mockApiCall(userShortlists);
  }
  
  return axios.get('/api/shortlists');
};

// API ENDPOINT: DELETE /api/shortlists/:shortlist_id
export const removeFromShortlist = async (shortlistId) => {
  if (USE_MOCK) {
    const index = mockShortlists.findIndex(s => s.shortlist_id === shortlistId);
    
    if (index === -1) {
      return mockApiError('Shortlist not found', 404);
    }
    
    mockShortlists.splice(index, 1);
    
    return mockApiCall({ message: 'Removed from shortlist' });
  }
  
  return axios.delete(`/api/shortlists/${shortlistId}`);
};
```

=================================================================
4. MOCK DATA STRUCTURES (Based on Database Schema)
=================================================================

All mock data follows the exact structure from homer_schema.sql

USER OBJECT:
```javascript
{
  user_id: 'user_xxx',
  email: 'user@example.com',
  phone: '+91xxxxxxxxxx',
  user_type: 'renter' | 'owner' | 'both',
  full_name: 'Full Name',
  profile_photo_url: 'url',
  date_of_birth: 'YYYY-MM-DD',
  gender: 'male' | 'female' | 'other',
  is_active: true,
  created_at: 'ISO timestamp',
  last_login: 'ISO timestamp',
  
  // Renter fields
  subscription_tier: 'free' | 'premium',
  subscription_start: 'ISO timestamp',
  subscription_end: 'ISO timestamp',
  is_verified_renter: boolean,
  renter_verification_status: 'none' | 'pending' | 'verified' | 'rejected',
  renter_verification_documents: { /* JSON */ },
  renter_preferences: { /* JSON */ },
  employment_info: { /* JSON */ },
  contacts_used: number,
  profile_visibility: boolean,
  
  // Owner fields
  is_verified_owner: boolean,
  owner_verification_status: 'none' | 'pending' | 'verified' | 'rejected'
}
```

PROPERTY OBJECT:
```javascript
{
  property_id: 'prop_xxx',
  owner_id: 'user_xxx',
  title: 'Property Title',
  description: 'Description',
  property_type: 'apartment' | 'villa' | 'independent_house' | 'pg',
  bhk_type: '1BHK' | '2BHK' | '3BHK' | '4BHK+',
  rent: number,
  security_deposit: number,
  maintenance_charges: number,
  location: {
    address: string,
    city: string,
    state: string,
    pincode: string,
    latitude: number,
    longitude: number,
    locality: string,
    landmarks: [string]
  },
  details: {
    carpet_area: number,
    total_floors: number,
    floor_number: number,
    furnishing: 'furnished' | 'semi-furnished' | 'unfurnished',
    parking: { car: number, bike: number },
    amenities: [string],
    bathrooms: number,
    balconies: number,
    facing: 'north' | 'south' | 'east' | 'west',
    age_of_property: number,
    available_from: 'YYYY-MM-DD',
    preferred_tenants: [string],
    pets_allowed: boolean,
    vegetarian_only: boolean
  },
  images: [string],
  video_url: string,
  virtual_tour_url: string,
  is_verified: boolean,
  verification_tier: 'free' | 'verified',
  verification_date: 'ISO timestamp',
  verification_documents: { /* JSON */ },
  lifestyle_data: {
    aqi_score: number,
    noise_level: number,
    walkability_score: number,
    nearby_parks: [{name: string, distance: number}],
    nearby_hospitals: [{name: string, distance: number}],
    nearby_schools: [{name: string, distance: number}],
    nearby_malls: [{name: string, distance: number}],
    public_transport_score: number,
    safety_score: number,
    calculated_at: 'ISO timestamp',
    is_mock: boolean
  },
  analytics: {
    total_views: number,
    premium_views: number,
    total_contacts: number,
    shortlisted_count: number,
    last_viewed: 'ISO timestamp'
  },
  status: 'active' | 'inactive' | 'rented' | 'deleted',
  created_at: 'ISO timestamp',
  updated_at: 'ISO timestamp'
}
```

CHAT OBJECT:
```javascript
{
  chat_id: 'chat_xxx',
  property_id: 'prop_xxx',
  renter_id: 'user_xxx',
  owner_id: 'user_xxx',
  initiated_by: 'renter' | 'owner',
  created_at: 'ISO timestamp',
  last_message_at: 'ISO timestamp',
  status: 'active' | 'archived' | 'blocked',
  messages: [
    {
      message_id: 'msg_xxx',
      sender_id: 'user_xxx',
      sender_type: 'renter' | 'owner',
      message: 'Message text',
      timestamp: 'ISO timestamp',
      is_read: boolean,
      attachments: [string]
    }
  ]
}
```

=================================================================
5. PAGE COMPONENTS & ROUTING
=================================================================

FILE: /app/frontend/src/App.js
-------------------------------

```javascript
import React from 'react';
import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';
import { AuthProvider } from './context/AuthContext';
import { NotificationProvider } from './context/NotificationContext';

// Public pages
import LandingPage from './pages/LandingPage';
import Login from './pages/Login';
import Signup from './pages/Signup';
import Search from './pages/Search';
import PropertyDetail from './pages/PropertyDetail';
import About from './pages/About';
import Pricing from './pages/Pricing';

// Renter pages
import RenterDashboard from './pages/renter/Dashboard';
import RenterProfile from './pages/renter/Profile';
import RenterSubscription from './pages/renter/Subscription';
import RenterShortlists from './pages/renter/Shortlists';
import RenterChats from './pages/renter/Chats';
import RenterVerification from './pages/renter/VerificationUpload';
import RenterPrivacy from './pages/renter/PrivacySettings';

// Owner pages
import OwnerDashboard from './pages/owner/Dashboard';
import OwnerProperties from './pages/owner/MyProperties';
import AddProperty from './pages/owner/AddProperty';
import EditProperty from './pages/owner/EditProperty';
import PropertyAnalytics from './pages/owner/PropertyAnalytics';
import OwnerVerification from './pages/owner/Verification';
import ReverseMarketplace from './pages/owner/ReverseMarketplace';
import OwnerChats from './pages/owner/Chats';

// Payment pages
import PaymentSuccess from './pages/PaymentSuccess';
import PaymentFailure from './pages/PaymentFailure';

// Layout
import Navbar from './components/layout/Navbar';
import Footer from './components/layout/Footer';

// Protected route wrapper
import ProtectedRoute from './components/common/ProtectedRoute';

function App() {
  return (
    <Router>
      <AuthProvider>
        <NotificationProvider>
          <div className="min-h-screen flex flex-col">
            <Navbar />
            
            <main className="flex-grow">
              <Routes>
                {/* Public routes */}
                <Route path="/" element={<LandingPage />} />
                <Route path="/login" element={<Login />} />
                <Route path="/signup" element={<Signup />} />
                <Route path="/search" element={<Search />} />
                <Route path="/property/:id" element={<PropertyDetail />} />
                <Route path="/about" element={<About />} />
                <Route path="/pricing" element={<Pricing />} />
                
                {/* Protected renter routes */}
                <Route path="/renter/dashboard" element={
                  <ProtectedRoute userType="renter">
                    <RenterDashboard />
                  </ProtectedRoute>
                } />
                <Route path="/renter/profile" element={
                  <ProtectedRoute userType="renter">
                    <RenterProfile />
                  </ProtectedRoute>
                } />
                <Route path="/renter/subscription" element={
                  <ProtectedRoute userType="renter">
                    <RenterSubscription />
                  </ProtectedRoute>
                } />
                <Route path="/renter/shortlists" element={
                  <ProtectedRoute userType="renter">
                    <RenterShortlists />
                  </ProtectedRoute>
                } />
                <Route path="/renter/chats" element={
                  <ProtectedRoute userType="renter">
                    <RenterChats />
                  </ProtectedRoute>
                } />
                <Route path="/renter/verification" element={
                  <ProtectedRoute userType="renter">
                    <RenterVerification />
                  </ProtectedRoute>
                } />
                <Route path="/renter/privacy" element={
                  <ProtectedRoute userType="renter">
                    <RenterPrivacy />
                  </ProtectedRoute>
                } />
                
                {/* Protected owner routes */}
                <Route path="/owner/dashboard" element={
                  <ProtectedRoute userType="owner">
                    <OwnerDashboard />
                  </ProtectedRoute>
                } />
                <Route path="/owner/properties" element={
                  <ProtectedRoute userType="owner">
                    <OwnerProperties />
                  </ProtectedRoute>
                } />
                <Route path="/owner/property/add" element={
                  <ProtectedRoute userType="owner">
                    <AddProperty />
                  </ProtectedRoute>
                } />
                <Route path="/owner/property/:id/edit" element={
                  <ProtectedRoute userType="owner">
                    <EditProperty />
                  </ProtectedRoute>
                } />
                <Route path="/owner/property/:id/analytics" element={
                  <ProtectedRoute userType="owner">
                    <PropertyAnalytics />
                  </ProtectedRoute>
                } />
                <Route path="/owner/verification" element={
                  <ProtectedRoute userType="owner">
                    <OwnerVerification />
                  </ProtectedRoute>
                } />
                <Route path="/owner/reverse-marketplace" element={
                  <ProtectedRoute userType="owner">
                    <ReverseMarketplace />
                  </ProtectedRoute>
                } />
                <Route path="/owner/chats" element={
                  <ProtectedRoute userType="owner">
                    <OwnerChats />
                  </ProtectedRoute>
                } />
                
                {/* Payment routes */}
                <Route path="/payment/success" element={<PaymentSuccess />} />
                <Route path="/payment/failure" element={<PaymentFailure />} />
              </Routes>
            </main>
            
            <Footer />
          </div>
        </NotificationProvider>
      </AuthProvider>
    </Router>
  );
}

export default App;
```

=================================================================
6. CONTEXT & STATE MANAGEMENT
=================================================================

FILE: /app/frontend/src/context/AuthContext.js
-----------------------------------------------

```javascript
import React, { createContext, useState, useEffect, useContext } from 'react';
import { login as loginAPI, logout as logoutAPI, getCurrentUser } from '../services/authService';

const AuthContext = createContext();

export const useAuth = () => {
  const context = useContext(AuthContext);
  if (!context) {
    throw new Error('useAuth must be used within AuthProvider');
  }
  return context;
};

export const AuthProvider = ({ children }) => {
  const [user, setUser] = useState(null);
  const [token, setToken] = useState(null);
  const [loading, setLoading] = useState(true);
  const [isAuthenticated, setIsAuthenticated] = useState(false);

  // Load user from localStorage on mount
  useEffect(() => {
    const loadUser = async () => {
      const storedToken = localStorage.getItem('token');
      const storedUser = localStorage.getItem('user');
      
      if (storedToken && storedUser) {
        setToken(storedToken);
        setUser(JSON.parse(storedUser));
        setIsAuthenticated(true);
      }
      
      setLoading(false);
    };
    
    loadUser();
  }, []);

  const login = async (credentials) => {
    try {
      const response = await loginAPI(credentials);
      const { token: newToken, user: userData } = response.data;
      
      setToken(newToken);
      setUser(userData);
      setIsAuthenticated(true);
      
      localStorage.setItem('token', newToken);
      localStorage.setItem('user', JSON.stringify(userData));
      
      return { success: true };
    } catch (error) {
      return { 
        success: false, 
        error: error.response?.data?.message || 'Login failed' 
      };
    }
  };

  const logout = async () => {
    try {
      await logoutAPI();
    } catch (error) {
      console.error('Logout error:', error);
    } finally {
      setToken(null);
      setUser(null);
      setIsAuthenticated(false);
      localStorage.removeItem('token');
      localStorage.removeItem('user');
    }
  };

  const updateUser = (userData) => {
    setUser(prevUser => ({ ...prevUser, ...userData }));
    localStorage.setItem('user', JSON.stringify({ ...user, ...userData }));
  };

  const value = {
    user,
    token,
    loading,
    isAuthenticated,
    login,
    logout,
    updateUser
  };

  return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>;
};
```

=================================================================
7. KEY UI COMPONENTS (Examples)
=================================================================

FILE: /app/frontend/src/components/property/PropertyCard.js
------------------------------------------------------------

```javascript
import React from 'react';
import { Link } from 'react-router-dom';
import { MapPin, BedDouble, Heart } from 'lucide-react';
import Badge from '../common/Badge';
import LifestyleScoreBadge from './LifestyleScoreBadge';

const PropertyCard = ({ property, onShortlist }) => {
  const {
    property_id,
    title,
    rent,
    location,
    bhk_type,
    details,
    images,
    is_verified,
    lifestyle_data
  } = property;

  return (
    <div className="bg-white rounded-lg shadow-md hover:shadow-xl transition-shadow duration-300 overflow-hidden">
      {/* Image */}
      <div className="relative h-48 overflow-hidden">
        <img
          src={images[0] || 'https://via.placeholder.com/400x300'}
          alt={title}
          className="w-full h-full object-cover hover:scale-105 transition-transform duration-300"
        />
        
        {/* Verification badge */}
        <div className="absolute top-2 right-2">
          {is_verified ? (
            <Badge variant="success">✓ Verified</Badge>
          ) : (
            <Badge variant="warning">Not Verified</Badge>
          )}
        </div>
        
        {/* Shortlist button */}
        <button
          onClick={() => onShortlist(property_id)}
          className="absolute top-2 left-2 bg-white p-2 rounded-full hover:bg-red-50 transition"
        >
          <Heart className="w-5 h-5 text-gray-600 hover:text-red-500" />
        </button>
      </div>

      {/* Content */}
      <Link to={`/property/${property_id}`}>
        <div className="p-4">
          {/* Title and Price */}
          <div className="flex justify-between items-start mb-2">
            <h3 className="text-lg font-semibold text-gray-900 line-clamp-2">
              {title}
            </h3>
            <div className="text-right ml-2">
              <p className="text-xl font-bold text-blue-600">₹{rent.toLocaleString()}</p>
              <p className="text-xs text-gray-500">per month</p>
            </div>
          </div>

          {/* Location */}
          <div className="flex items-center text-gray-600 mb-3">
            <MapPin className="w-4 h-4 mr-1" />
            <span className="text-sm">{location.locality}, {location.city}</span>
          </div>

          {/* Details */}
          <div className="flex items-center text-gray-700 mb-3">
            <BedDouble className="w-4 h-4 mr-2" />
            <span className="text-sm">{bhk_type}</span>
            <span className="mx-2">•</span>
            <span className="text-sm">{details.furnishing}</span>
            <span className="mx-2">•</span>
            <span className="text-sm">{details.carpet_area} sq.ft</span>
          </div>

          {/* Lifestyle scores (if verified) */}
          {lifestyle_data && (
            <div className="flex flex-wrap gap-2">
              <LifestyleScoreBadge 
                type="aqi" 
                value={lifestyle_data.aqi_score} 
                size="small"
              />
              <LifestyleScoreBadge 
                type="walkability" 
                value={lifestyle_data.walkability_score} 
                size="small"
              />
            </div>
          )}
        </div>
      </Link>
    </div>
  );
};

export default PropertyCard;
```

FILE: /app/frontend/src/components/upsell/UpgradeModal.js
----------------------------------------------------------

```javascript
import React from 'react';
import { X, Check } from 'lucide-react';
import Modal from '../common/Modal';
import Button from '../common/Button';

const UpgradeModal = ({ isOpen, onClose, onUpgrade }) => {
  const freeFeatures = [
    '5 property contacts',
    'Basic search filters',
    'Browse all listings',
    'Shortlist properties'
  ];

  const premiumFeatures = [
    'Unlimited property contacts',
    'Verified Renter badge',
    'Advanced lifestyle search',
    'Natural language search',
    'Reverse marketplace visibility',
    'Priority support'
  ];

  return (
    <Modal isOpen={isOpen} onClose={onClose} size="large">
      <div className="p-6">
        {/* Header */}
        <div className="flex justify-between items-center mb-6">
          <h2 className="text-2xl font-bold text-gray-900">
            Upgrade to Premium
          </h2>
          <button onClick={onClose} className="text-gray-400 hover:text-gray-600">
            <X className="w-6 h-6" />
          </button>
        </div>

        {/* Content */}
        <div className="mb-6">
          <p className="text-red-600 font-semibold mb-4">
            ⚠️ You've reached your contact limit (5/5)
          </p>
          <p className="text-gray-600 mb-6">
            Upgrade to Premium to unlock unlimited contacts and powerful search features!
          </p>

          {/* Feature comparison */}
          <div className="grid md:grid-cols-2 gap-6">
            {/* Free tier */}
            <div className="border border-gray-200 rounded-lg p-4">
              <h3 className="text-lg font-semibold mb-4">Free</h3>
              <ul className="space-y-2">
                {freeFeatures.map((feature, index) => (
                  <li key={index} className="flex items-start">
                    <Check className="w-5 h-5 text-gray-400 mr-2 flex-shrink-0 mt-0.5" />
                    <span className="text-gray-600">{feature}</span>
                  </li>
                ))}
              </ul>
            </div>

            {/* Premium tier */}
            <div className="border-2 border-blue-500 rounded-lg p-4 bg-blue-50">
              <div className="flex justify-between items-center mb-4">
                <h3 className="text-lg font-semibold text-blue-600">Premium</h3>
                <span className="bg-blue-600 text-white px-3 py-1 rounded-full text-sm">
                  ₹750 / 90 days
                </span>
              </div>
              <ul className="space-y-2">
                {premiumFeatures.map((feature, index) => (
                  <li key={index} className="flex items-start">
                    <Check className="w-5 h-5 text-blue-600 mr-2 flex-shrink-0 mt-0.5" />
                    <span className="text-gray-700 font-medium">{feature}</span>
                  </li>
                ))}
              </ul>
            </div>
          </div>
        </div>

        {/* Actions */}
        <div className="flex gap-3">
          <Button variant="outline" onClick={onClose} className="flex-1">
            Maybe Later
          </Button>
          <Button onClick={onUpgrade} className="flex-1 bg-blue-600 hover:bg-blue-700">
            Upgrade Now - ₹750
          </Button>
        </div>
      </div>
    </Modal>
  );
};

export default UpgradeModal;
```

=================================================================
8. TESTING WITHOUT BACKEND
=================================================================

TESTING CHECKLIST:

1. AUTHENTICATION FLOW:
   □ Can register new user (stored in mock data)
   □ Can login with registered credentials
   □ Token stored in localStorage
   □ User persists after page refresh
   □ Can logout (clears localStorage)

2. PROPERTY BROWSING:
   □ Search page loads with mock properties
   □ Filters work (price, BHK, location)
   □ Property cards display correctly
   □ Can click to view property detail
   □ Property detail page shows all information
   □ Images display correctly

3. CONTACT FLOW (Free Tier):
   □ Contact counter shows correctly (X/5)
   □ Can initiate chat with property
   □ Chat appears in chat list
   □ After 5 contacts, upgrade modal appears
   □ Cannot contact 6th property without upgrade

4. PREMIUM UPGRADE:
   □ Click upgrade shows subscription page
   □ Mock payment modal opens
   □ Payment completes (2 second delay)
   □ User tier updates to "premium"
   □ Contacts reset to 0
   □ Can now contact unlimited properties
   □ Premium badge appears

5. SHORTLIST:
   □ Can add property to shortlist
   □ Shortlist page shows saved properties
   □ Can remove from shortlist

6. CHAT:
   □ Chat list shows all conversations
   □ Can open chat window
   □ Can send messages
   □ Messages appear in chat
   □ Unread count updates

7. OWNER FEATURES:
   □ Can create new property
   □ Multi-step form works
   □ Image upload simulated
   □ Property appears in "My Properties"
   □ Can edit property
   □ Analytics shows mock data

8. LIFESTYLE SEARCH:
   □ Advanced filters show for premium users
   □ Can filter by AQI, noise, walkability
   □ Results show only verified properties
   □ Lifestyle scores display correctly

9. UI/UX:
   □ All pages responsive on mobile
   □ Loading states show correctly
   □ Error messages display
   □ Success toasts appear
   □ Animations smooth

10. DATA PERSISTENCE:
    □ Mock data persists during session
    □ LocalStorage works correctly
    □ No console errors

=================================================================
9. SWITCHING TO REAL BACKEND
=================================================================

When backend is ready, ONLY change this:

1. In .env file:
   ```
   REACT_APP_USE_MOCK_DATA=false
   ```

2. That's it! All API calls will now go to real backend.

The API service layer automatically switches between mock and real
based on the USE_MOCK flag. No code changes needed.

Make sure backend implements exact same API contracts defined in
Section 3 of this document.

=================================================================
10. ADMIN DASHBOARD (FULL DATABASE CONTROL)
=================================================================

ADMIN DASHBOARD FEATURES:
- Complete control over all database entities
- CRUD operations for all tables
- User management (view, edit, delete users)
- Property management (approve, reject, edit, delete)
- Verification management (approve/reject with admin override)
- Transaction history and financial reports
- Analytics and insights
- System settings and configuration

FILE: /app/frontend/src/pages/admin/AdminDashboard.js
-------------------------------------------------------

```javascript
import React, { useState, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { 
  Users, Home, CheckCircle, DollarSign, 
  Settings, Database, Shield, BarChart3 
} from 'lucide-react';

const AdminDashboard = () => {
  const [stats, setStats] = useState({
    totalUsers: 0,
    totalProperties: 0,
    pendingVerifications: 0,
    totalRevenue: 0
  });

  const menuItems = [
    { icon: Users, label: 'User Management', path: '/admin/users', color: 'blue' },
    { icon: Home, label: 'Property Management', path: '/admin/properties', color: 'green' },
    { icon: CheckCircle, label: 'Verifications', path: '/admin/verifications', color: 'purple' },
    { icon: DollarSign, label: 'Transactions', path: '/admin/transactions', color: 'yellow' },
    { icon: BarChart3, label: 'Analytics', path: '/admin/analytics', color: 'red' },
    { icon: Database, label: 'Database Tools', path: '/admin/database', color: 'indigo' },
    { icon: Settings, label: 'System Settings', path: '/admin/settings', color: 'gray' }
  ];

  return (
    <div className="min-h-screen bg-gray-50 p-6">
      <div className="max-w-7xl mx-auto">
        {/* Header */}
        <div className="flex items-center justify-between mb-8">
          <div>
            <h1 className="text-3xl font-bold text-gray-900">Admin Dashboard</h1>
            <p className="text-gray-600 mt-1">Complete system control and management</p>
          </div>
          <Shield className="w-12 h-12 text-red-600" />
        </div>

        {/* Stats Grid */}
        <div className="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
          <StatCard label="Total Users" value={stats.totalUsers} color="blue" />
          <StatCard label="Total Properties" value={stats.totalProperties} color="green" />
          <StatCard label="Pending Verifications" value={stats.pendingVerifications} color="purple" />
          <StatCard label="Revenue" value={`₹${stats.totalRevenue.toLocaleString()}`} color="yellow" />
        </div>

        {/* Menu Grid */}
        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
          {menuItems.map((item, index) => (
            <MenuCard key={index} {...item} />
          ))}
        </div>
      </div>
    </div>
  );
};

export default AdminDashboard;
```

FILE: /app/frontend/src/pages/admin/UserManagement.js
------------------------------------------------------

```javascript
import React, { useState, useEffect } from 'react';
import { getUsersAdmin, updateUserAdmin, deleteUserAdmin } from '../../services/adminService';

const UserManagement = () => {
  const [users, setUsers] = useState([]);
  const [filters, setFilters] = useState({
    user_type: 'all',
    subscription_tier: 'all',
    verification_status: 'all'
  });
  const [selectedUser, setSelectedUser] = useState(null);
  const [showEditModal, setShowEditModal] = useState(false);

  // Features:
  // - View all users with filters
  // - Edit user details (name, email, phone, subscription, verification status)
  // - Delete users
  // - Bulk operations
  // - Export user data
  // - Search by email/phone/name

  return (
    <div className="admin-user-management">
      {/* User table with actions */}
      {/* Edit modal */}
      {/* Bulk actions */}
    </div>
  );
};

export default UserManagement;
```

FILE: /app/frontend/src/pages/admin/PropertyManagement.js
----------------------------------------------------------

```javascript
import React, { useState, useEffect } from 'react';
import { getPropertiesAdmin, updatePropertyAdmin, deletePropertyAdmin } from '../../services/adminService';

const PropertyManagement = () => {
  // Features:
  // - View all properties
  // - Edit property details
  // - Delete properties
  // - Force verify/unverify
  // - Change property status (active/inactive/rented)
  // - Bulk operations
  // - Analytics per property

  return (
    <div className="admin-property-management">
      {/* Property table with actions */}
    </div>
  );
};

export default PropertyManagement;
```

FILE: /app/frontend/src/pages/admin/VerificationManagement.js
--------------------------------------------------------------

```javascript
import React, { useState, useEffect } from 'react';
import { 
  getPendingVerifications, 
  approveVerification, 
  rejectVerification,
  overrideVerification 
} from '../../services/adminService';

const VerificationManagement = () => {
  // Features:
  // - View all pending verifications (renters & properties)
  // - Approve/Reject with reasons
  // - Admin override (force verify without documents)
  // - View verification history
  // - Document viewer
  // - Bulk approve/reject

  return (
    <div className="admin-verification-management">
      {/* Verification queue */}
    </div>
  );
};

export default VerificationManagement;
```

FILE: /app/frontend/src/pages/admin/TransactionManagement.js
-------------------------------------------------------------

```javascript
import React, { useState, useEffect } from 'react';
import { getTransactionsAdmin, refundTransaction } from '../../services/adminService';

const TransactionManagement = () => {
  // Features:
  // - View all transactions
  // - Filter by type, status, user
  // - Issue refunds
  // - Mark as success/failed
  // - Export financial reports
  // - Revenue analytics

  return (
    <div className="admin-transaction-management">
      {/* Transaction table */}
    </div>
  );
};

export default TransactionManagement;
```

FILE: /app/frontend/src/pages/admin/DatabaseTools.js
-----------------------------------------------------

```javascript
import React, { useState } from 'react';
import { executeDatabaseQuery, backupDatabase } from '../../services/adminService';

const DatabaseTools = () => {
  // Features:
  // - Direct database query interface
  // - View all tables
  // - Run custom SQL queries (read-only by default)
  // - Database backup
  // - Export data as CSV/JSON
  // - Data integrity checks

  return (
    <div className="admin-database-tools">
      {/* Query interface */}
      {/* Table browser */}
    </div>
  );
};

export default DatabaseTools;
```

FILE: /app/frontend/src/services/adminService.js
-------------------------------------------------

```javascript
import { USE_MOCK, API_BASE_URL, mockApiCall, mockApiError } from './mockApi';
import axios from 'axios';
import { mockUsers, mockProperties, mockTransactions } from './mockData';

// =================================================================
// USER MANAGEMENT APIS
// =================================================================

// API ENDPOINT: GET /api/admin/users
export const getUsersAdmin = async (filters = {}) => {
  if (USE_MOCK) {
    let users = [...mockUsers];
    
    if (filters.user_type && filters.user_type !== 'all') {
      users = users.filter(u => u.user_type === filters.user_type);
    }
    
    if (filters.subscription_tier && filters.subscription_tier !== 'all') {
      users = users.filter(u => u.subscription_tier === filters.subscription_tier);
    }
    
    return mockApiCall({ users, total_count: users.length });
  }
  
  return axios.get('/api/admin/users', { params: filters });
};

// API ENDPOINT: PUT /api/admin/users/:user_id
export const updateUserAdmin = async (userId, userData) => {
  if (USE_MOCK) {
    const index = mockUsers.findIndex(u => u.user_id === userId);
    
    if (index === -1) {
      return mockApiError('User not found', 404);
    }
    
    mockUsers[index] = { ...mockUsers[index], ...userData, updated_at: new Date().toISOString() };
    
    return mockApiCall({ user: mockUsers[index] });
  }
  
  return axios.put(`/api/admin/users/${userId}`, userData);
};

// API ENDPOINT: DELETE /api/admin/users/:user_id
export const deleteUserAdmin = async (userId) => {
  if (USE_MOCK) {
    const index = mockUsers.findIndex(u => u.user_id === userId);
    
    if (index === -1) {
      return mockApiError('User not found', 404);
    }
    
    mockUsers.splice(index, 1);
    
    return mockApiCall({ message: 'User deleted successfully' });
  }
  
  return axios.delete(`/api/admin/users/${userId}`);
};

// =================================================================
// PROPERTY MANAGEMENT APIS
// =================================================================

// API ENDPOINT: GET /api/admin/properties
export const getPropertiesAdmin = async (filters = {}) => {
  if (USE_MOCK) {
    let properties = [...mockProperties];
    
    // Apply admin filters
    if (filters.status) {
      properties = properties.filter(p => p.status === filters.status);
    }
    
    return mockApiCall({ properties, total_count: properties.length });
  }
  
  return axios.get('/api/admin/properties', { params: filters });
};

// API ENDPOINT: PUT /api/admin/properties/:property_id
export const updatePropertyAdmin = async (propertyId, propertyData) => {
  if (USE_MOCK) {
    const index = mockProperties.findIndex(p => p.property_id === propertyId);
    
    if (index === -1) {
      return mockApiError('Property not found', 404);
    }
    
    mockProperties[index] = { ...mockProperties[index], ...propertyData };
    
    return mockApiCall({ property: mockProperties[index] });
  }
  
  return axios.put(`/api/admin/properties/${propertyId}`, propertyData);
};

// API ENDPOINT: DELETE /api/admin/properties/:property_id
export const deletePropertyAdmin = async (propertyId) => {
  if (USE_MOCK) {
    const index = mockProperties.findIndex(p => p.property_id === propertyId);
    
    if (index === -1) {
      return mockApiError('Property not found', 404);
    }
    
    mockProperties.splice(index, 1);
    
    return mockApiCall({ message: 'Property deleted successfully' });
  }
  
  return axios.delete(`/api/admin/properties/${propertyId}`);
};

// API ENDPOINT: POST /api/admin/properties/:property_id/force-verify
export const forceVerifyProperty = async (propertyId) => {
  if (USE_MOCK) {
    const property = mockProperties.find(p => p.property_id === propertyId);
    
    if (!property) {
      return mockApiError('Property not found', 404);
    }
    
    property.is_verified = true;
    property.verification_tier = 'verified';
    property.verification_date = new Date().toISOString();
    
    return mockApiCall({ property });
  }
  
  return axios.post(`/api/admin/properties/${propertyId}/force-verify`);
};

// =================================================================
// VERIFICATION MANAGEMENT APIS
// =================================================================

// API ENDPOINT: GET /api/admin/verifications/pending
export const getPendingVerifications = async () => {
  if (USE_MOCK) {
    const pendingUsers = mockUsers.filter(
      u => u.renter_verification_status === 'pending' || u.owner_verification_status === 'pending'
    );
    
    const pendingProperties = mockProperties.filter(
      p => !p.is_verified && p.verification_tier === 'pending'
    );
    
    return mockApiCall({
      pending_users: pendingUsers,
      pending_properties: pendingProperties
    });
  }
  
  return axios.get('/api/admin/verifications/pending');
};

// API ENDPOINT: POST /api/admin/verifications/approve
export const approveVerification = async (type, id) => {
  if (USE_MOCK) {
    if (type === 'user') {
      const user = mockUsers.find(u => u.user_id === id);
      if (user) {
        if (user.user_type === 'renter') {
          user.renter_verification_status = 'verified';
          user.is_verified_renter = true;
        } else {
          user.owner_verification_status = 'verified';
          user.is_verified_owner = true;
        }
      }
    } else if (type === 'property') {
      const property = mockProperties.find(p => p.property_id === id);
      if (property) {
        property.is_verified = true;
        property.verification_tier = 'verified';
        property.verification_date = new Date().toISOString();
      }
    }
    
    return mockApiCall({ message: 'Verification approved' });
  }
  
  return axios.post('/api/admin/verifications/approve', { type, id });
};

// API ENDPOINT: POST /api/admin/verifications/reject
export const rejectVerification = async (type, id, reason) => {
  if (USE_MOCK) {
    if (type === 'user') {
      const user = mockUsers.find(u => u.user_id === id);
      if (user) {
        if (user.user_type === 'renter') {
          user.renter_verification_status = 'rejected';
        } else {
          user.owner_verification_status = 'rejected';
        }
      }
    }
    
    return mockApiCall({ message: 'Verification rejected', reason });
  }
  
  return axios.post('/api/admin/verifications/reject', { type, id, reason });
};

// =================================================================
// TRANSACTION MANAGEMENT APIS
// =================================================================

// API ENDPOINT: GET /api/admin/transactions
export const getTransactionsAdmin = async (filters = {}) => {
  if (USE_MOCK) {
    let transactions = [...mockTransactions];
    
    if (filters.transaction_type) {
      transactions = transactions.filter(t => t.transaction_type === filters.transaction_type);
    }
    
    if (filters.payment_status) {
      transactions = transactions.filter(t => t.payment_status === filters.payment_status);
    }
    
    return mockApiCall({ transactions, total_count: transactions.length });
  }
  
  return axios.get('/api/admin/transactions', { params: filters });
};

// API ENDPOINT: POST /api/admin/transactions/:transaction_id/refund
export const refundTransaction = async (transactionId) => {
  if (USE_MOCK) {
    const transaction = mockTransactions.find(t => t.transaction_id === transactionId);
    
    if (!transaction) {
      return mockApiError('Transaction not found', 404);
    }
    
    transaction.payment_status = 'refunded';
    transaction.refunded_at = new Date().toISOString();
    
    return mockApiCall({ transaction });
  }
  
  return axios.post(`/api/admin/transactions/${transactionId}/refund`);
};

// =================================================================
// DATABASE TOOLS APIS
// =================================================================

// API ENDPOINT: POST /api/admin/database/query
export const executeDatabaseQuery = async (query, readOnly = true) => {
  if (USE_MOCK) {
    // Mock: Only return sample data, don't actually execute
    return mockApiCall({
      results: [],
      message: 'Query executed successfully (mock)',
      rowCount: 0
    });
  }
  
  return axios.post('/api/admin/database/query', { query, readOnly });
};

// API ENDPOINT: POST /api/admin/database/backup
export const backupDatabase = async () => {
  if (USE_MOCK) {
    return mockApiCall({
      backup_url: 'mock_backup_url.sql',
      message: 'Database backup created'
    });
  }
  
  return axios.post('/api/admin/database/backup');
};

export default {
  getUsersAdmin,
  updateUserAdmin,
  deleteUserAdmin,
  getPropertiesAdmin,
  updatePropertyAdmin,
  deletePropertyAdmin,
  forceVerifyProperty,
  getPendingVerifications,
  approveVerification,
  rejectVerification,
  getTransactionsAdmin,
  refundTransaction,
  executeDatabaseQuery,
  backupDatabase
};
```

ADMIN ROUTES (Add to App.js):
```javascript
// Admin routes (protected)
<Route path="/admin/dashboard" element={
  <ProtectedRoute userType="admin">
    <AdminDashboard />
  </ProtectedRoute>
} />
<Route path="/admin/users" element={
  <ProtectedRoute userType="admin">
    <UserManagement />
  </ProtectedRoute>
} />
<Route path="/admin/properties" element={
  <ProtectedRoute userType="admin">
    <PropertyManagement />
  </ProtectedRoute>
} />
<Route path="/admin/verifications" element={
  <ProtectedRoute userType="admin">
    <VerificationManagement />
  </ProtectedRoute>
} />
<Route path="/admin/transactions" element={
  <ProtectedRoute userType="admin">
    <TransactionManagement />
  </ProtectedRoute>
} />
<Route path="/admin/database" element={
  <ProtectedRoute userType="admin">
    <DatabaseTools />
  </ProtectedRoute>
} />
```

ADMIN AUTHENTICATION:
- Add admin user type to database
- Admin login with special credentials
- Admin token with elevated permissions
- Protected routes check for admin role

TESTING ADMIN DASHBOARD:
□ Admin can login
□ Dashboard shows system stats
□ Can view all users
□ Can edit user details
□ Can delete users
□ Can view all properties
□ Can edit/delete properties
□ Can force verify properties
□ Can approve/reject verifications
□ Can view all transactions
□ Can issue refunds
□ Can access database tools
□ All changes reflect in database

=================================================================
END OF FRONTEND WORKFLOW
=================================================================

This frontend is 100% functional with mock data and ready to
integrate with backend when available.

All API contracts match the database schema from homer_schema.sql.
```
